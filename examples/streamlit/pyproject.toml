[project]
name = "vamos-streamlit-example"
version = "0.1.0"
description = "Minimal Streamlit applet for Vamos process-control smoke testing"
requires-python = ">=3.11"
dependencies = [
  "streamlit==1.46.1",
]
