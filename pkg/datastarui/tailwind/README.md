# DatastarUI Tailwind inputs

`utilities.css` contains theme-neutral utilities required by copied DatastarUI components.

Consumers copy this file through the `datastarui` CLI and import it from their app Tailwind entrypoint. App theme tokens and app-specific composition CSS stay in the consuming app, not in DatastarUI copied defaults.
