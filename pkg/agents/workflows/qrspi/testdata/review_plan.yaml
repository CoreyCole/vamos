```yaml
qrspi_result:
  stage: "review-plan"
  status: "complete"
  outcome: "ready-for-workspace"
  policy:
    auto_mode: false
    enable_plan_reviews: true
    invalid_result_retry_limit: 1
  summary:
    plan_goal: "Move QRSPI result contract from XML to YAML."
    stage_completed: "Reviewed plan and fixed planning docs."
    key_decisions: "Next stage should start immediately: q-workspace."
  artifact: "thoughts/example/reviews/plan-review/review.md"
  next:
    steps:
      - action: "read_skill"
        param: ".pi/skills/q-workspace/SKILL.md"
      - action: "start_stage"
        param: "q-workspace"
```
